# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Supabase CLI temp files
supabase/.temp/

# 캐릭터 자산 — 베타 단계는 WebP만 git 포함(Vercel 배포용), PNG 원본은 제외.
# 4종 확장 + Supabase Storage 업로드 후 전체 폴더를 다시 ignore 전환 예정.
public/character-assets-local/**/*.png

# Env files — contains project-specific config; commit .env.example instead if needed
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
